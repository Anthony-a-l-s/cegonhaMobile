//Estilos de Botões para as páginas

import {StyleSheet, Dimensions} from 'react-native';
const windowWidth = Dimensions.get('window').width;
const windowHeight = Dimensions.get('window').height;
export const ButtonStyles = StyleSheet.create({
  customButton: {
    width: '100%',
    alignSelf: 'center',
    borderRadius: 34,
    height: '100%',
    justifyContent: 'center',
  },

  customButton2: {
    width: '30%',
    alignSelf: 'center',
    borderRadius: 34,
    height: '100%',
    justifyContent: 'center',
  },
  //Estilos para InformationScreen
  coloredbutton: {
    width: windowWidth * 0.15,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton2: {
    width: windowWidth * 0.15,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton3: {
    width: windowWidth * 0.15,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton4: {
    width: windowWidth * 0.15,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: windowHeight * 0.02,
  },
  //

  //Estilos para SupportScreen
  coloredbutton5: {
    width: windowWidth * 0.16,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton6: {
    width: windowWidth * 0.16,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton7: {
    width: windowWidth * 0.16,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  //

  //Estilos para AdminScreen
  coloredbutton8: {
    width: windowWidth * 0.18,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton9: {
    width: windowWidth * 0.18,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#47CACC',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton10: {
    width: windowWidth * 0.18,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#E7B7C8',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton11: {
    width: windowWidth * 0.18,
    height: windowHeight * 0.045,
    borderRadius: 34,
    backgroundColor: '#CDB3D4',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },
  coloredbutton12: {
    width: windowWidth * 0.30,
    height: windowHeight * 0.035,
    borderRadius: 34,
    backgroundColor: '#282a36',
    alignItems: 'center',
    justifyContent: 'center',

    marginTop: windowHeight * 0.02,
  },

  coloredbutton13: {
    marginBottom: 10,
    width: '80%',
    height: 50, 
    backgroundColor: '#7BE495',
    margin: 10, 
    borderRadius: 30
  },

  //
});
